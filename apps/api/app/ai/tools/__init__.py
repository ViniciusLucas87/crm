"""MCP-compatible tool definitions.

Defines tools that can be exposed to AI agents via the Model Context Protocol.
Each tool follows the standard MCP tool schema with name, description, and
input schema, enabling AI agents to interact with the CRM system.
"""
