# Contracts

This package stores TypeScript contracts shared by the web app.

Python equivalents live in `apps/api/app/domain`.
Until code generation is introduced, keep both schemas aligned when changing fields.
